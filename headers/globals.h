#pragma once
#include <SDL2/SDL.h>  
  SDL_Window *window;// SDL_CreateWindow("Dots", 0, 0, ScWidth, ScHeight, SDL_WINDOW_RESIZABLE);;
  SDL_Renderer *renderer;// SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED); ;
  SDL_Surface *surface;
  SDL_Event event;
  SDL_Event event2;

  



 
